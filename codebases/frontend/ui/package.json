{
  "name": "@dealpme/ui",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Design system DealPME : jetons de la charte (Marine Encre, Bleu Signal, Papier, Acier ; Barlow / Barlow Condensed) et composants nommés de la Tranche 3 (AppShell, TransactionRail, PermissionLens, StateBanner, WarningStrip, DecisionGate...).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src/tokens.css"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "peerDependencies": {
    "react": "^19.1.0"
  },
  "devDependencies": {
    "@dealpme/config": "*",
    "@types/react": "^19.1.12",
    "react": "^19.1.1"
  }
}
