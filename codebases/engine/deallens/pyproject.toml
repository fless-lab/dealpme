[project]
name = "dealpme-deallens"
version = "0.1.0"
description = "DealLens : assistant IA de diligence, pare-feu de permission avant récupération (V5)."
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
api = ["fastapi>=0.115", "uvicorn>=0.30", "pydantic>=2.8"]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
