{
  "name": "@dealpme/connector-antivirus",
  "description": "Analyse antivirus des pièces déposées (ClamAV en local et en production). Aucun fichier n'est mis à disposition avant un verdict propre.",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@dealpme/config": "*"
  }
}
