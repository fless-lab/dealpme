{
  "name": "@dealpme/connector-storage",
  "description": "Stockage objet compatible S3 (MinIO en local). Chiffrement côté serveur, versionnage, aucun accès public, URL pré-signées courtes liées à la session.",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@dealpme/config": "*"
  }
}
